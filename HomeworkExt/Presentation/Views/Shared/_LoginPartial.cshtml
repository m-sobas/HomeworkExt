@using Microsoft.AspNetCore.Identity
@using HomeworkExt.Core.Models.Domains;
@inject SignInManager<ApplicationUser> SignInManager
@inject UserManager<ApplicationUser> UserManager


<ul class="nav navbar-nav navbar-right">
	@if (SignInManager.IsSignedIn(User))
	{
		<li>
			@*<a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @User.Identity.Name!</a>*@
			<a href="">@User.Identity.Name</a>
		</li>
		<li>
			<a asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Car", new { area = "" })">
			<span class="glyphicon glyphicon-log-out"></span> Wyloguj</a>

@*			<form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Announcement", new { area = "" })">
				<button type="submit" class="glyphicon glyphicon-log-in"> Wyloguj</button>
			</form>
*@		</li>
	}
	else
	{
		<li>
			<a asp-area="Identity" asp-page="/Account/Register"><span class="glyphicon glyphicon-user"></span> Rejestracja</a>
		</li>
		<li>
			<a asp-area="Identity" asp-page="/Account/Login"><span class="glyphicon glyphicon-log-in"></span> Zaloguj się</a>
		</li>
	}
</ul>
